# Core dependencies
numpy>=1.21.0
scipy>=1.7.0
matplotlib>=3.4.0
pandas>=1.3.0
PyWavelets==1.4.1
scikit-learn>=1.2.0

# Build tools
pyinstaller>=5.7.0

# Note: tkinter is part of Python standard library
# and should not be pip installed

# Optional AI features (graceful fallback if missing)
# If installed, enables sentence-transformers embeddings and document parsing for PDF/DOCX
sentence-transformers>=2.2.0
pdfplumber>=0.10.0
python-docx>=1.0.0